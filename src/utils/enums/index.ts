export enum OrderStatus {
  PENDING = 'pending',
  PROCESSING = 'processing',
  SHIPPED = 'shipped',
  DELIVERED = 'delivered',
  CANCELED = 'canceled',
}

export enum VoucherType {
  FREE_SHIP = 'free_ship',
  DISCOUNT = 'discount',
}
